function replaceHtmlContent (element_id, innerHTML) {
  const elem = document.getElementById(element_id);

  if (elem){
    elem.innerHTML = innerHTML;
  }
}

replaceHtmlContent('books-tbody', "<%= j render @books %>");
replaceHtmlContent('kaminari-infos', "<%= j render 'layouts/shared/kaminari_infos', list: @books, remote: true %>")
replaceHtmlContent('nav-bar-books', "<%= j render 'books/nav_bar' %>")
replaceHtmlContent('order-books', "<%= j render 'books/order' %>")
<%# replaceHtmlContent('filters-books', "j render 'books/filters'") %>

window.history.pushState('Livros', "Página <%= params['page']%>", "<%= j request.original_url %>".replaceAll("amp;", ""));

$(document).ready(function() {
  submit_form_ajax_input_text('form-books-filter', 'contains_text');
  submit_form_ajax_input_text('form-books-filter', 'with_author');
  submit_form_ajax_input_select('form-books-order', 'order_by');
});
